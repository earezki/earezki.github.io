{
  "name": "earezki-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-cv-markdown.js && astro build && node scripts/generate-pdf.js",
    "preview": "astro preview",
    "postbuild": "node scripts/postbuild.js",
    "migrate:posts": "node scripts/migrate-posts.js"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.1.2",
    "astro": "^4.10.0",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "gray-matter": "^4.0.3",
    "puppeteer": "^24.27.0"
  }
}
