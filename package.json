{
  "name": "earezki-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/minify-redirect.js && node scripts/detect-new-articles.js && node scripts/generate-cv-markdown.js && astro build && node scripts/generate-pdf.js",
    "preview": "astro preview",
    "postbuild": "node scripts/postbuild.js",
    "migrate:posts": "node scripts/migrate-posts.js"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.13",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.15.8",
    "unist-util-visit": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "gray-matter": "^4.0.3",
    "puppeteer": "^24.30.0",
    "terser": "^5.44.1"
  }
}
